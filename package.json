{
  "name": "doc-reels",
  "private": true,
  "workspaces": ["api", "web"],
  "scripts": {
    "dev": "concurrently \"bun run --cwd api src/index.ts\" \"bun run --cwd web dev\"",
    "demo": "concurrently \"bun run --cwd api src/index.ts\" \"bun run --cwd web vite --mode demo\"",
    "split": "bun run scripts/split-videos.ts",
    "install:all": "bun install --cwd api && bun install --cwd web"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
